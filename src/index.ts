/**
 * @amlplugins/twilio-sync
 *
 * Thin re-export of twilio (CommonJS `export =` SDK).
 *
 * Twilio Sync — real-time state synchronization across devices via Documents, Lists, Maps, Streams.
 */

import _sdk from "twilio";
export { _sdk as sdk };
export default _sdk;
